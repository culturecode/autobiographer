<%= content_tag :div, :class => 'comment clickable', :id => dom_id(comment) do %>
    <%= event_timestamp(comment.event) %>
    <div class='body'>
        <div class='content'><%= event_service_icon(comment.event) %><%= comment.text %></div>
    </div>
<% end %>