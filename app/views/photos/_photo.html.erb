<%= content_tag :span, :class => 'photo' do %>
    <%= link_to(image_tag(photo.file.thumb.url), photo.file.url, :rel => dom_id(photo.photo_group), :class => 'thumbnail') %><%= delete_photo_link(photo) %>
<% end %>