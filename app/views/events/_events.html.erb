<% events.each_with_index do |event, index| %>
    <%= content_tag :li, render(:partial => 'events/event', :locals => {:event => event, :timestamp => event_list_timestamp(event, events[index-1])}), :class => :event, :id => dom_id(event) %>
<% end%>    
