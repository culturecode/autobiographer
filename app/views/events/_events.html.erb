<% events.each_with_index do |event, index| %>
    <%= render :partial => '/events/event', :locals => {:event => event, :options => {:timestamp => event_list_timestamp(event, events[index-1])}}%>
<% end%>    

